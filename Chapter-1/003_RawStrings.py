# The r prefix makes this a raw string, so backslashes are treated literally
# and are NOT needed to escape the double quotes here (though escaping still
# works inside a raw string when quoting the same delimiter).
print(r"Hello \"World!\" Dunia")